Potential improvements:
This is not scaled to mobile/tablet sizes and will not work on those view screens.  
The sliding form will especially break, as it is using magic numbers instead of a percentage that will properly shift.

While I used a form element for the form, the way I have things setup prevents the form verification from working.
An improvement could be to have the button's function only trigger after first trying to submit the form locally.

The visuals on this site are also quite plain, but that is a minor problem.
